{
  "name": "animx-monorepo",
  "version": "0.1.0",
  "packageManager": "npm@11.6.2",
  "private": true,
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "dev:web": "turbo dev --filter=web"
  },
  "devDependencies": {
    "turbo": "^2.5.0"
  }
}
